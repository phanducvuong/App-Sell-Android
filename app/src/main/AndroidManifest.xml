<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sellapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.CartDetailActivity"></activity>
        <activity android:name=".View.ProductDetailActivity" />
        <activity android:name=".View.ProductByCateMenuActivity" />
        <activity android:name=".View.FashionByMenuActivity" />
        <activity
            android:name=".View.CommentActivity"
            android:theme="@style/Base.Theme.MaterialComponents.Dialog" />
        <activity android:name=".View.CommentDetailActivity" />
        <activity
            android:name=".View.HelloScreenActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.HomeActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>